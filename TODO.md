# TODO

- [ ] **Внешний вид на красивую обновить.** Сейчас всё работает, но выглядит спартански:
  - TUI (`clipshield tui` / `:ClipshieldTUI`): заголовок окна (`title` в `nvim_open_win`),
    подсветка имени и замены разными группами ((`name` — Identifier, `→ replacement` — String),
    полужирный номер), легенда в winbar с иконками, счётчик записей в углу, рамка/стиль под
    winborder пользователя.
  - wofi-менеджер: свой CSS (`.config/wofi/style.css` уже, вероятно, есть под их тему) —
    сейчас окно системное дефолтное; приглушить строки-подсказки (header/разделитель) цветом.
  - zenity-диалог: единственный некрасивый элемент, GTK-дефолт; вариант — заменить на ввод
    прямо в wofi dmenu (если подтвердится возврат набранного текста) или на второй TUI-флоат.
  - notify-send: иконка и urgency=normal вместо безликого текста.
